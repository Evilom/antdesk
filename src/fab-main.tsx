import React from 'react'
import ReactDOM from 'react-dom/client'
import FAB from './fab'

ReactDOM.createRoot(document.getElementById('fab-root')!).render(
  <React.StrictMode>
    <FAB />
  </React.StrictMode>,
)
